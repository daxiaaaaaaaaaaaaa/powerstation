<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <!-- 填充 -->
    <solid android:color="@color/color_btn_deful" />
    <stroke
        android:width="0.5dp"
        android:color="@color/color_btn_deful"/>
    <!-- 边框宽度和颜色 -->
</shape>